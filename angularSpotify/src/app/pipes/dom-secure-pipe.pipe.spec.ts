import { DomSecurePipePipe } from './dom-secure-pipe.pipe';

describe('DomSecurePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DomSecurePipePipe();
    expect(pipe).toBeTruthy();
  });
});
