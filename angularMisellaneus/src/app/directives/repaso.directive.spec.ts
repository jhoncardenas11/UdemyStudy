import { RepasoDirective } from './repaso.directive';

describe('RepasoDirective', () => {
  it('should create an instance', () => {
    const directive = new RepasoDirective();
    expect(directive).toBeTruthy();
  });
});
